public class Exe {
	// Execution of the program begins here
	public static void main(String[] args) {
		
		// Construction of an Object dynamically -> at run time -> Heap is involved as memory
		Student s1 = new Student();
								// Initial Values for the Object
		Student s2 = new Student("Jennie",20,"jennie@example.com",'F',"XYZ Address");
		// s1 -> Reference Variable constructed at compile time i.e. goes in stack
		// new is an operator which creates the object on the heap i.e. dynamically at run time
		// Student() -> Call to default Constructor
		int i = 10;
		System.out.println("i is: "+i);
		System.out.println("s1 is: "+s1);
		System.out.println("s2 is: "+s2);
		
		// Writing the data into Object
		//s1.setName("John");
		//s1.setAge(20);
		//s1.setEmail("john@example.com");
		//s1.setGender('M');
		//s1.setAddress("ABC Address");
		
		System.out.println("Contents within object of s1 are:");
		
		// Reading the data from Object
		System.out.println(s1.getName()+" - "+s1.getAge()+" - "+s1.getEmail()+" - "+s1.getGender()+" - "+s1.getAddress());
		
		
		System.out.println("Contents within object of s2 are:");
		s2.setAddress("PQR Address");
		// Reading the data from Object
		System.out.println(s2.getName()+" - "+s2.getAge()+" - "+s2.getEmail()+" - "+s2.getGender()+" - "+s2.getAddress());
		
		
	}
}
