package com.company;

import java.util.TreeMap;

/**
 * 664. Strange Printer
 *      There is a strange printer with the following two special requirements:
 *      1. printer can only print a sequence of the same character each time
 *      2. at each time, the printer can print new characters starting from and
 *      ending at any places, and will cover the original existing characters
 *
 *      Given a string constists of lower English letters only, your job is to count
 *      the minimum number of turns the printer needed in order to print it
 *
 *      Example:
 *          Input: "aaabbb"
 *          Output: "2"
 *          Explain: print "aaa" first and print "bbb"
 */
public class Strange_Printer__664 {
    //Somehow using DP to solve this
    public int strangePrinter(String s){

    }
}
